spring:
  application:
    name: boca-demo-sentinel
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 #nacos配置中心地址
        group: boca_group #nacos配置分组
        file-extension: yaml #nacos配置文件类型